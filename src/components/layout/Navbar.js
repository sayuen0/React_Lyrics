import React, { Component } from "react";

export default class Navbar extends Component {
  render() {
    return (
      <nav className="navbar navbar-dark bg-dark mg-5">
        <span className="navbar-brand mb-0 h1 mx-auto">歌詞見つけるで。</span>
      </nav>
    );
  }
}
